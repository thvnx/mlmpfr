language: c

install:
  - sudo apt-get install ocaml
  - sudo apt-get install opam
  - opam init -a
  - opam update
  - opam install oasis

script:
  - oasis setup
  - ./configure --enable-tests
  - make
  - make doc
  - make tests

